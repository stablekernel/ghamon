# GHA Monitor (ghamon)

GHA Monitor (ghamon) is a TUI program that monitors a given GitHub Actions workflow for one or more GitHub repositories. See spec.md for design and implementation details.


## Build

To build the application, run the following command:

```bash
mage build
```

## Test

To run the unit tests, execute:

```bash
mage test
```
